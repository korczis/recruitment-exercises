# PubSub module (optional)
defmodule EqLabs.PubSub do
  def publish(topic, data) do
    # publish data to topic
  end

  def subscribe(topic, callback) do
    # subscribe to topic and call callback on data update
  end
end